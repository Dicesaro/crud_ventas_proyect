<p style="color: green"><%= notice %></p>

<div class="show_cliente">
<%= render @cliente %>
<div class="botones">
  <%= link_to "Editar cliente ✏️", edit_cliente_path(@cliente), class: "btn_edit" %> 
  <%= link_to "Ir a clientes 👈", clientes_path %>
</div>
  <%= button_to "Eliminar cliente 🗑️", @cliente, method: :delete %>
</div>
